# Maze Pro 기술 상세 및 AI 연동 명세

## 1. AI API 엔드포인트 명세
**초기화 API (`/api/v1/maze/init`)**
- **기능**: 게임 시작 시 생성된 전체 미로(2D 배열)를 AI의 시스템 메시지에 학습시킵니다.
- **포맷**: `{"map": [["#", " ", "C"], ...]}` 형태로 전달되며, 서버는 이를 평문으로 변환해 LangChain Memory에 저장합니다. 

**명령 실행 API (`/api/v1/maze`)**
- **기능**: 플레이어의 자연어 명령과 현재 게임 상태(플레이어 좌표, 크리스탈 위치)를 전송합니다.
- **응답**: `{"thought": "...", "moves": [{"direction": "right", "steps": 1}]}` 구조의 JSON을 반환하며, 클라이언트는 이를 파싱하여 이동 로직을 수행합니다.

## 2. LangChain 및 모델 설정 (`index.mjs`)
- **모델**: `llama3.2:3b` (Ollama를 통한 로컬 구동)
- **파라미터 설정 전략**:
  - `temperature: 0`: AI의 창의성을 완전히 배제하고 항상 결정론적(Deterministic)이고 안정적인 이동 경로만 응답하도록 강제합니다.
  - `maxTokens: 200`: 실시간 게임 환경에 맞게 빠른 응답 속도를 확보하기 위해 출력 길이를 제한합니다.
  - `top_k: 1`: 가장 확률이 높은 토큰만 샘플링해 JSON 포맷 오류나 잘못된 방향(벽 통과 등)을 방지합니다.

## 3. 미로 생성 알고리즘 (Iterative DFS)
- **원리**: 2칸 단위로 무작위 방향으로 가지를 뻗어나가는 재귀적 백트래킹(Recursive Backtracking) 알고리즘을 사용합니다.
- **특징**: 생성된 모든 통로가 하나로 연결된 "완벽한 미로(Perfect Maze)"를 보장합니다. 따라서 닫힌 루프(Loop)나 플레이어가 갇히는 고립된 공간이 절대 발생하지 않습니다.

## 4. 난이도 설정 (Difficulty Config)
| 난이도 | 그리드 크기(Grid) | 크리스탈 개수 | 타일 총합 | 체감 복잡도 |
|--------|-----------|-------------|---------|------------|
| Easy | 11 x 11 | 3개 | 121개 | 낮음 |
| Medium | 17 x 17 | 5개 | 289개 | 중간 |
| Hard | 25 x 25 | 10개 | 625개 | 높음 |
